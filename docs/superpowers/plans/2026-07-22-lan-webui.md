# 局域网 WebUI 与安全隧道 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Farm_Go 设置中提供密码保护的局域网和成品 FRP 安全隧道 WebUI，并复用现有 React 控制页面。

**Architecture:** `internal/lanaccess` 只负责监听、认证会话与 HTTP 安全边界；主包只负责设置热切换、嵌入资源和逐方法 RPC 适配。浏览器在 React 渲染前安装兼容 Wails 绑定的 HTTP 代理，桌面端继续使用原生 Wails IPC。

**Tech Stack:** Go `net/http`、`net/netip`、`crypto/rand`、`golang.org/x/crypto/argon2`；SQLite；Wails v2；React、TypeScript、Vite、Vitest。

---

## File Structure

- Create: `internal/lanaccess/config.go`, `auth.go`, `server.go` 及对应 `*_test.go`：监听策略、Argon2id 密码、会话、CSRF、HTTP 防护。
- Create: `internal/storage/lan_access.go`, `lan_access_test.go`：全局 LAN 设置与不含 hash 的公开 DTO。
- Create: `lan_access.go`, `lan_rpc.go` 及测试：应用生命周期、热切换、嵌入资源和显式业务白名单。
- Modify: `app.go`, `main.go`：在启动和关闭时管理 LAN 服务，将静态资源和轮询处理器传给它。
- Create: `frontend/src/lib/remoteBridge.ts`, `frontend/src/RemoteApp.tsx` 及测试：远程密码页和 Wails 兼容 HTTP 调用。
- Modify: `frontend/src/main.tsx`, `LicenseGate.tsx`, `AuthorizedApp.tsx`, `components/AppShell.tsx`：远程启动、授权页复用和远程标签限制。
- Create: `frontend/src/components/LANAccessSettingsPanel.tsx` 及测试；Modify `views/SettingsView.tsx`：系统设置入口。
- Modify: `frontend/src/style.css`, `README.md`：远程移动端底部导航和 FRP 配置说明。

### Task 1: 配置模型和来源地址策略

**Files:**
- Create: `internal/lanaccess/config.go`
- Create: `internal/lanaccess/config_test.go`

- [ ] **Step 1: 写失败测试。**

```go
func TestConfigValidateRejectsUnsafeValues(t *testing.T) {
    for _, cfg := range []Config{
        {Enabled: true, Mode: ModeLAN, Port: 1023},
        {Enabled: true, Mode: ModeLAN, Port: 65536},
        {Enabled: true, Mode: "public", Port: 8788},
        {Enabled: true, Mode: ModeTunnel, Port: 8788},
    } {
        if err := cfg.Validate(); err == nil { t.Fatalf("accepted %#v", cfg) }
    }
}
func TestAllowsRemoteAddrUsesModeBoundary(t *testing.T) {
    if !AllowsRemoteAddr(ModeLAN, "192.168.1.4:1000") || AllowsRemoteAddr(ModeLAN, "8.8.8.8:1000") { t.Fatal("LAN boundary wrong") }
    if !AllowsRemoteAddr(ModeTunnel, "127.0.0.1:1000") || AllowsRemoteAddr(ModeTunnel, "192.168.1.4:1000") { t.Fatal("tunnel boundary wrong") }
}
```

- [ ] **Step 2: 运行失败测试。**

Run: `go test ./internal/lanaccess -run 'TestConfigValidateRejectsUnsafeValues|TestAllowsRemoteAddrUsesModeBoundary' -count=1`

Expected: FAIL，`Config`、`ModeLAN` 或 `AllowsRemoteAddr` 未定义。

- [ ] **Step 3: 编写最小实现。**

```go
type Mode string
const ( ModeLAN Mode = "lan"; ModeTunnel Mode = "tunnel" )
type Config struct { Enabled bool; Mode Mode; Port int; PasswordHash string }
func (c Config) ListenAddr() string { if c.Mode == ModeTunnel { return net.JoinHostPort("127.0.0.1", strconv.Itoa(c.Port)) }; return net.JoinHostPort("0.0.0.0", strconv.Itoa(c.Port)) }
func (c Config) Validate() error {
    if !c.Enabled { return nil }
    if c.Mode != ModeLAN && c.Mode != ModeTunnel { return fmt.Errorf("unsupported LAN access mode %q", c.Mode) }
    if c.Port < 1024 || c.Port > 65535 { return fmt.Errorf("LAN access port %d is outside 1024-65535", c.Port) }
    if c.PasswordHash == "" { return errors.New("LAN access password is required") }
    return nil
}
func AllowsRemoteAddr(mode Mode, remote string) bool {
    host, _, err := net.SplitHostPort(remote); if err != nil { return false }
    addr, err := netip.ParseAddr(host); if err != nil || addr.Is6() { return false }
    if mode == ModeTunnel { return addr.IsLoopback() }
    return mode == ModeLAN && (addr.IsLoopback() || addr.IsPrivate())
}
```

拒绝 IPv6、非私有 IPv4 和无法解析的 `RemoteAddr`；不得读取转发头。

- [ ] **Step 4: 运行包测试。**

Run: `go test ./internal/lanaccess -count=1`

Expected: PASS。

- [ ] **Step 5: 提交。**

Run: `git add internal/lanaccess/config.go internal/lanaccess/config_test.go`

Run: `git commit -m "feat: add LAN access listener policy"`

### Task 2: 持久化不泄密的 LAN 设置

**Files:**
- Create: `internal/storage/lan_access.go`
- Create: `internal/storage/lan_access_test.go`

- [ ] **Step 1: 写失败测试。**

```go
func TestLANAccessSettingsRoundTripKeepsHashOutOfPublicDTO(t *testing.T) {
    store, err := Open(context.Background(), t.TempDir()); if err != nil { t.Fatal(err) }; defer store.Close()
    want := LANAccessSettings{Enabled: true, Mode: LANAccessModeTunnel, Port: 8788, PasswordHash: "argon2id$secret"}
    if err := store.SaveLANAccessSettings(context.Background(), want); err != nil { t.Fatal(err) }
    got, err := store.LoadLANAccessSettings(context.Background()); if err != nil { t.Fatal(err) }
    if !reflect.DeepEqual(got, want) { t.Fatalf("got %#v want %#v", got, want) }
    if strings.Contains(fmt.Sprintf("%#v", got.Public()), "secret") { t.Fatal("public DTO leaked hash") }
}
```

- [ ] **Step 2: 运行失败测试。**

Run: `go test ./internal/storage -run TestLANAccessSettingsRoundTripKeepsHashOutOfPublicDTO -count=1`

Expected: FAIL，LAN 设置 API 未定义。

- [ ] **Step 3: 实现存储记录。**

在全局 `settings` 账户读写 `lanAccess.enabled`、`lanAccess.mode`、`lanAccess.port` 和 `lanAccess.passwordHash`。实现以下类型，默认关闭、模式 `lan`、端口 `8788`：

```go
type LANAccessSettings struct { Enabled bool; Mode LANAccessMode; Port int; PasswordHash string }
type LANAccessPublicSettings struct { Enabled bool `json:"enabled"`; Mode LANAccessMode `json:"mode"`; Port int `json:"port"`; PasswordConfigured bool `json:"passwordConfigured"` }
func (s LANAccessSettings) Public() LANAccessPublicSettings { return LANAccessPublicSettings{Enabled:s.Enabled, Mode:s.Mode, Port:s.Port, PasswordConfigured:s.PasswordHash != ""} }
```

- [ ] **Step 4: 运行存储包测试。**

Run: `go test ./internal/storage -count=1`

Expected: PASS。

- [ ] **Step 5: 提交。**

Run: `git add internal/storage/lan_access.go internal/storage/lan_access_test.go`

Run: `git commit -m "feat: persist LAN access settings"`

### Task 3: 密码、会话、CSRF 与失败限制

**Files:**
- Create: `internal/lanaccess/auth.go`
- Create: `internal/lanaccess/auth_test.go`

- [ ] **Step 1: 写失败测试。**

```go
func TestAuthenticatorRevokesSessionsAfterPasswordChange(t *testing.T) {
    auth := NewAuthenticator(testClock)
    hash, _ := HashPassword("correct horse battery staple")
    auth.SetPasswordHash(hash)
    session, err := auth.Login("correct horse battery staple", "192.168.1.5", ModeLAN)
    if err != nil || !auth.Authorize(session.ID, session.CSRF) { t.Fatal("login failed") }
    auth.SetPasswordHash(mustHash(t, "new correct horse battery staple"))
    if auth.Authorize(session.ID, session.CSRF) { t.Fatal("old session remained authorized") }
}
func TestAuthenticatorLocksLANIPAfterFiveFailures(t *testing.T) {
    auth := NewAuthenticator(testClock); auth.SetPasswordHash(mustHash(t, "correct horse battery staple"))
    for attempt := 0; attempt < 5; attempt++ {
        if _, err := auth.Login("wrong", "192.168.1.5", ModeLAN); !errors.Is(err, ErrInvalidPassword) { t.Fatalf("attempt %d: %v", attempt, err) }
    }
    if _, err := auth.Login("wrong", "192.168.1.5", ModeLAN); !errors.Is(err, ErrLocked) { t.Fatalf("sixth failure = %v", err) }
}
func TestAuthenticatorUsesProgressiveTunnelBackoff(t *testing.T) {
    auth := NewAuthenticator(testClock); auth.SetPasswordHash(mustHash(t, "correct horse battery staple"))
    for _, want := range []time.Duration{time.Second, 2*time.Second, 4*time.Second, 8*time.Second} {
        _, err := auth.Login("wrong", "127.0.0.1", ModeTunnel)
        var retry RetryError
        if !errors.As(err, &retry) || retry.After != want { t.Fatalf("retry = %#v, want %s", retry, want) }
        testClock.Advance(want)
    }
}
```

- [ ] **Step 2: 运行失败测试。**

Run: `go test ./internal/lanaccess -run TestAuthenticator -count=1`

Expected: FAIL，认证器 API 未定义。

- [ ] **Step 3: 实现认证器。**

实现 `HashPassword`、`VerifyPassword`、`Login`、`Authorize`、`RevokeAll` 和 `SetPasswordHash`。使用 `argon2.IDKey`，随机 16-byte salt、3 iterations、64 MiB、4 lanes、32-byte key，并以 `argon2id$v=1$m=65536,t=3,p=4$...` 保存。使用 `crypto/rand` 生成 32-byte 会话和 CSRF，内存会话固定 12 小时。LAN 以真实 TCP IP 计数，五次失败锁 15 分钟；隧道以 `tunnel` 键返回 1/2/4/8/16/32/60 秒渐进 `RetryAfter`，不读取任何转发头。

- [ ] **Step 4: 运行认证与竞态测试。**

Run: `go test -race ./internal/lanaccess -count=1`

Expected: PASS 且无 race report。

- [ ] **Step 5: 提交。**

Run: `git add internal/lanaccess/auth.go internal/lanaccess/auth_test.go`

Run: `git commit -m "feat: secure LAN access authentication"`

### Task 4: 防绕过 HTTP 服务

**Files:**
- Create: `internal/lanaccess/server.go`
- Create: `internal/lanaccess/server_test.go`

- [ ] **Step 1: 写失败测试。**

```go
func TestServerRejectsRPCWithoutSessionEvenWhenStaticAppLoads(t *testing.T) {
    server := newTestServer(t, ModeLAN)
    req := httptest.NewRequest(http.MethodPost, "/api/rpc/FarmAutomationState", strings.NewReader(`{"args":[]}`))
    req.RemoteAddr = "192.168.1.10:3210"
    rec := httptest.NewRecorder(); server.ServeHTTP(rec, req)
    if rec.Code != http.StatusUnauthorized { t.Fatalf("status=%d", rec.Code) }
}
func TestServerRejectsForgedCookieMissingCSRFAndUnknownMethod(t *testing.T) {
    server, session := newAuthenticatedTestServer(t, ModeLAN)
    forged := httptest.NewRequest(http.MethodPost, "/api/rpc/FarmAutomationState", strings.NewReader(`{"args":[]}`))
    forged.RemoteAddr = "192.168.1.10:3210"; forged.AddCookie(&http.Cookie{Name:"farm_go_lan", Value:"forged"})
    if rec := httptest.NewRecorder(); server.ServeHTTP(rec, forged); rec.Code != http.StatusUnauthorized { t.Fatalf("forged=%d", rec.Code) }
    missingCSRF := httptest.NewRequest(http.MethodPost, "/api/rpc/FarmAutomationState", strings.NewReader(`{"args":[]}`))
    missingCSRF.RemoteAddr = "192.168.1.10:3210"; missingCSRF.Host = "farm.test"; missingCSRF.Header.Set("Origin", "http://farm.test"); missingCSRF.AddCookie(&http.Cookie{Name:"farm_go_lan", Value:session.ID})
    if rec := httptest.NewRecorder(); server.ServeHTTP(rec, missingCSRF); rec.Code != http.StatusForbidden { t.Fatalf("missing csrf=%d", rec.Code) }
    unknown := httptest.NewRequest(http.MethodPost, "/api/rpc/ExitApplication", strings.NewReader(`{"args":[]}`))
    unknown.RemoteAddr = "192.168.1.10:3210"; unknown.Host = "farm.test"; unknown.Header.Set("Origin", "http://farm.test"); unknown.Header.Set("X-Farm-Go-CSRF", session.CSRF); unknown.AddCookie(&http.Cookie{Name:"farm_go_lan", Value:session.ID})
    if rec := httptest.NewRecorder(); server.ServeHTTP(rec, unknown); rec.Code != http.StatusNotFound { t.Fatalf("unknown=%d", rec.Code) }
}
func TestServerRejectsPublicLANSourceAndNonLoopbackTunnelSource(t *testing.T) {
    for _, tc := range []struct{ mode Mode; remote string }{{ModeLAN, "8.8.8.8:1"}, {ModeTunnel, "192.168.1.5:1"}} {
        server := newTestServer(t, tc.mode)
        req := httptest.NewRequest(http.MethodGet, "/api/auth/session", nil); req.RemoteAddr = tc.remote
        if rec := httptest.NewRecorder(); server.ServeHTTP(rec, req); rec.Code != http.StatusForbidden { t.Fatalf("%s %s: %d", tc.mode, tc.remote, rec.Code) }
    }
}
```

- [ ] **Step 2: 运行失败测试。**

Run: `go test ./internal/lanaccess -run TestServer -count=1`

Expected: FAIL，`NewServer` 未定义。

- [ ] **Step 3: 实现服务和中间件。**

定义 `RPCDispatcher`：

```go
type RPCDispatcher interface { Dispatch(context.Context, string, []json.RawMessage) (any, error) }
type Options struct { Config Config; Auth *Authenticator; Assets fs.FS; RPC RPCDispatcher; Poll http.Handler }
```

注册 `POST /api/auth/login`、`POST /api/auth/logout`、`GET /api/auth/session`、`POST /api/rpc/{method}` 及受会话保护的轮询前缀。先检查来源，后检查会话；非登录 POST 再要求 `Origin == scheme + "://" + r.Host` 和 `X-Farm-Go-CSRF`。Cookie 名为 `farm_go_lan`，使用 `HttpOnly`、`SameSiteStrict`、`MaxAge:43200`，安全隧道模式设置 `Secure:true`。设置 `Cache-Control:no-store`、1 MiB JSON 上限、无 CORS、`nosniff`、`DENY`、`no-referrer` 和同源 CSP。静态文件只能显示密码页，不能绕过 API 鉴权。

- [ ] **Step 4: 运行安全测试。**

Run: `go test -race ./internal/lanaccess -count=1`

Expected: PASS。

- [ ] **Step 5: 提交。**

Run: `git add internal/lanaccess/server.go internal/lanaccess/server_test.go`

Run: `git commit -m "feat: add guarded LAN WebUI server"`

### Task 5: 应用生命周期、嵌入资源和热切换

**Files:**
- Create: `lan_access.go`, `lan_access_test.go`
- Modify: `app.go: App struct, startup, shutdown`
- Modify: `main.go: asset middleware and startup wiring`

- [ ] **Step 1: 写失败测试。**

```go
func TestSaveLANAccessSettingsKeepsRunningServerWhenCandidatePortIsBusy(t *testing.T) {
    app := newAuthorizedTestApp(t); app.store = openTestStore(t)
    old := enableLANAccess(t, app, ModeLAN)
    blocker, _ := net.Listen("tcp", "127.0.0.1:0"); defer blocker.Close()
    _, err := app.SaveLANAccessSettings(LANAccessSettingsInput{Enabled:true, Mode:"lan", Port:blocker.Addr().(*net.TCPAddr).Port})
    if err == nil { t.Fatal("busy port accepted") }
    if got := app.LANAccessSettings(); got.Port != old.Port || !got.Running { t.Fatalf("old server lost: %#v", got) }
}
```

- [ ] **Step 2: 运行失败测试。**

Run: `go test . -run TestSaveLANAccessSettingsKeepsRunningServerWhenCandidatePortIsBusy -count=1`

Expected: FAIL，App LAN API 未定义。

- [ ] **Step 3: 实现 App 适配。**

在 `lan_access.go` 定义 Wails 输入 `LANAccessSettingsInput{Enabled, Mode, Port, Password, ConfirmPassword}` 与无 hash 的 `LANAccessStatus{Enabled, Mode, Port, PasswordConfigured, Running, Address, Error}`。`SaveLANAccessSettings` 验证 12 位密码与确认值；空密码仅在已有 hash 时保留；启用且无密码时返回错误。`lanAccessManager.Apply` 必须先监听候选服务、成功持久化后再替换旧服务；候选监听或存储失败时关闭候选并保留旧服务和旧配置。`startup` 恢复已启用服务，`shutdown` 先关闭它；`main.go` 传入 `assets`、`newRuntimePollHandler(newAppRuntimePollService(app))` 与 Task 6 dispatcher。

- [ ] **Step 4: 运行主包和存储测试。**

Run: `go test . ./internal/storage -count=1`

Expected: PASS。

- [ ] **Step 5: 提交。**

Run: `git add app.go main.go lan_access.go lan_access_test.go internal/storage`

Run: `git commit -m "feat: manage LAN WebUI from application settings"`

### Task 6: 显式业务 RPC 白名单

**Files:**
- Create: `lan_rpc.go`, `lan_rpc_test.go`

- [ ] **Step 1: 写失败测试。**

```go
func TestLANRPCRejectsDesktopOnlyMethods(t *testing.T) {
    dispatcher := newLANRPCDispatcher(newAuthorizedTestApp(t))
    for _, method := range []string{"ExitApplication", "MinimizeToTray", "SaveTextFile", "InstallQQDebugPatch", "LaunchHostProcess", "RestartHostProcess"} {
        if _, err := dispatcher.Dispatch(context.Background(), method, nil); !errors.Is(err, errLANRPCMethodNotAllowed) { t.Fatalf("%s: %v", method, err) }
    }
}
```

- [ ] **Step 2: 运行失败测试。**

Run: `go test . -run TestLANRPC -count=1`

Expected: FAIL，dispatcher 未定义。

- [ ] **Step 3: 实现逐方法 dispatcher。**

禁止 `reflect`。使用 `map[string]func(context.Context, []json.RawMessage) (any,error)` 以及 `decodeNoArgs`、`decodeOne[T]`、`decodeTwo[A,B]`。允许账户确认、状态、自动化、土地、仓库和社交业务：`CurrentRuntimeAccount`、`IdentifyRuntimeAccount`、`ConfirmRuntimeAccount`、`FarmAccountStatus`、`FarmAutomationState`、`FarmAutomationSchedulerState`、`ApplyRecommendedFarmAutomationConfig`、`RunFarmAutomationTask`、`RunDueFarmAutomationTasks`、`StartFarmAutomationScheduler`、`StopFarmAutomationScheduler`、`SaveFarmAutomationState`、`FarmFeatureCatalog`、`FarmCropAnalytics`、`FarmAtlasPreview`、`FarmAtlasBuyLockedPreview`、`FarmAtlasBuyLockedCrops`、`FarmFertilizeLand`、`FarmLandRush`、`FarmShovelLands`、`FarmBackpackSeedOptions`、`FarmWarehouse`、`FarmWarehouseRefresh`、`FarmWarehouseSell`、`FarmWarehouseSellRecords`、`WarehouseAutoSellSettings`、`SaveWarehouseAutoSellSettings`、`FarmSocialState`、`FarmSocialAction`、`FarmSocialRankings`、`FarmSocialRefreshVisitors`、`FarmSocialRankingPreferences`、`SaveFarmSocialRankingPreferences`、`FarmSocialDogGuardState`、`FarmSocialDogGuardAction`、`FarmSocialProtocolBlockList`、`FarmSocialExport`、`FarmSocialImport`、`FarmStealCropOptions`。未知方法返回 `errLANRPCMethodNotAllowed`，HTTP 层映射为 404；参数错误映射为 400。

- [ ] **Step 4: 运行 RPC 与轮询回归测试。**

Run: `go test . -run 'TestLANRPC|TestRuntimePollHandler' -count=1`

Expected: PASS。

- [ ] **Step 5: 提交。**

Run: `git add lan_rpc.go lan_rpc_test.go`

Run: `git commit -m "feat: restrict remote WebUI RPC methods"`

### Task 7: 浏览器密码门和 Wails 兼容桥接

**Files:**
- Create: `frontend/src/lib/remoteBridge.ts`, `remoteBridge.test.ts`
- Create: `frontend/src/RemoteApp.tsx`, `RemoteApp.test.tsx`
- Modify: `frontend/src/main.tsx`, `LicenseGate.tsx`, `LicenseGate.test.tsx`

- [ ] **Step 1: 写失败测试。**

```ts
it('posts a Wails-style call with the session CSRF token', async () => {
  installRemoteBridge({ csrfToken: 'csrf', fetchImpl });
  await window.go.main.App.FarmAutomationState();
  expect(fetchImpl).toHaveBeenCalledWith('/api/rpc/FarmAutomationState', expect.objectContaining({ method: 'POST', headers: expect.objectContaining({ 'X-Farm-Go-CSRF': 'csrf' }) }));
});
it('renders the reused credential gate as an access-password form', () => {
  expect(renderToStaticMarkup(<RemoteApp />)).toContain('验证访问密码');
});
```

- [ ] **Step 2: 运行失败测试。**

Run: `npm test -- --run src/lib/remoteBridge.test.ts src/RemoteApp.test.tsx`

Expected: FAIL，模块不存在。

- [ ] **Step 3: 实现远程启动。**

服务返回的 index 注入 `window.__FARM_GO_REMOTE__=true`。`main.tsx` 在 `root.render` 前调用 `installRemoteBridge()`；仅在该 flag 下使用 `Proxy` 创建 `window.go.main.App`，把 `{args}` 发送到同源 `/api/rpc/<method>`，携带 `credentials:'same-origin'` 和 `X-Farm-Go-CSRF`，非 2xx 转为 Error。抽取 `LicenseGate` 的共同视觉组件（title、fieldLabel、description、submitLabel、rememberVisible）；桌面继续传“卡密验证”并保留记住卡密，`RemoteApp` 用“验证访问密码”、不显示记住项、调用 `/api/auth/login`，成功后才渲染 `AuthorizedApp`。

- [ ] **Step 4: 运行前端测试和构建。**

Run: `npm test -- --run src/LicenseGate.test.tsx src/lib/remoteBridge.test.ts src/RemoteApp.test.tsx`

Run: `npm run build`

Expected: 两个命令 exit code 0。

- [ ] **Step 5: 提交。**

Run: `git add frontend/src/main.tsx frontend/src/LicenseGate.tsx frontend/src/RemoteApp.tsx frontend/src/lib/remoteBridge.ts frontend/src/LicenseGate.test.tsx frontend/src/RemoteApp.test.tsx frontend/src/lib/remoteBridge.test.ts`

Run: `git commit -m "feat: add authenticated browser bridge"`

### Task 8: 系统设置与远程界面限制

**Files:**
- Create: `frontend/src/components/LANAccessSettingsPanel.tsx`, `LANAccessSettingsPanel.test.tsx`
- Modify: `frontend/src/AuthorizedApp.tsx`, `components/AppShell.tsx`, `components/AppShell.test.tsx`
- Modify: `frontend/src/views/SettingsView.tsx`, `SettingsView.test.tsx`

- [ ] **Step 1: 写失败测试。**

```ts
it('hides desktop-only tabs when AppShell is remote', () => {
  const html = renderToStaticMarkup(<AppShell remote activeTab="workspace" onTabChange={() => undefined}>x</AppShell>);
  expect(html).not.toContain('系统设置');
  expect(html).not.toContain('日志中心');
  expect(html).toContain('农场自动化');
});
it('requires matching passwords of at least 12 characters before enabling LAN access', async () => {
  const save = vi.fn();
  const view = create(<LANAccessSettingsPanel initial={{ enabled: false, mode: 'lan', port: 8788, passwordConfigured: false }} onSave={save} />);
  act(() => view.root.findByProps({ name: 'lan-access-enabled' }).props.onChange({ target: { checked: true } }));
  act(() => view.root.findByProps({ name: 'lan-access-password' }).props.onChange({ target: { value: 'short' } }));
  act(() => view.root.findByProps({ name: 'lan-access-confirm-password' }).props.onChange({ target: { value: 'different' } }));
  await act(async () => { await view.root.findByProps({ name: 'save-lan-access' }).props.onClick() });
  expect(save).not.toHaveBeenCalled();
  expect(JSON.stringify(view.toJSON())).toContain('至少 12 个字符且两次输入一致');
});
```

- [ ] **Step 2: 运行失败测试。**

Run: `npm test -- --run src/components/AppShell.test.tsx src/components/LANAccessSettingsPanel.test.tsx src/views/SettingsView.test.tsx`

Expected: FAIL，remote prop 或 LAN 面板不存在。

- [ ] **Step 3: 实现 UI。**

为 `AppShell` 和 `AuthorizedApp` 加 `remote?: boolean`。远程仅保留工作台、农场自动化、资产与土地、好友社交、账户状态；不渲染守护、日志、消息推送、设置、启动注入和更新下载。传递 `remote` 到 `FarmWorkspaceView`，隐藏本机文件保存按钮，保留内存中的名单文本导入。`LANAccessSettingsPanel` 调用 Wails `LANAccessSettings` 与 `SaveLANAccessSettings`，校验端口 1024-65535 和密码确认，保存后清空本地密码 state，展示 LAN URL 或 `127.0.0.1:<port>` FRP 目标、运行状态和后端错误。将其作为 `SettingsView` 第一块设置区。

- [ ] **Step 4: 运行设置测试并生成绑定。**

Run: `npm test -- --run src/components/AppShell.test.tsx src/components/LANAccessSettingsPanel.test.tsx src/views/SettingsView.test.tsx`

Run: `wails generate module`

Run: `npm run build`

Expected: 三个命令 exit code 0。

- [ ] **Step 5: 提交。**

Run: `git add frontend/src frontend/wailsjs`

Run: `git commit -m "feat: add LAN access settings and remote UI limits"`

### Task 9: 远程移动端适配和最终验证

**Files:**
- Modify: `frontend/src/style.css`, `frontend/src/components/AppShell.test.tsx`, `README.md`

- [ ] **Step 1: 写失败断言。**

```ts
it('marks remote navigation for the mobile bottom bar stylesheet', () => {
  const html = renderToStaticMarkup(<AppShell remote activeTab="workspace" onTabChange={() => undefined}>x</AppShell>);
  const css = readFileSync(new URL('../style.css', import.meta.url), 'utf8');
  expect(html).toContain('app-shell-remote');
  expect(css).toContain('.app-shell-remote .sidebar');
  expect(css).toContain('position: fixed');
});
```

- [ ] **Step 2: 运行失败测试。**

Run: `npm test -- --run src/components/AppShell.test.tsx`

Expected: FAIL，远程样式选择器不存在。

- [ ] **Step 3: 实现窄屏布局和文档。**

在 `@media (max-width:760px)` 仅对 `.app-shell-remote` 预留底部空间，将 `.sidebar` 固定至底部，`.sidebar-nav` 用等宽横向网格并可横向滚动，隐藏品牌与账户块。远程弹窗使用 `width:100vw;height:100dvh;border-radius:0`，保留各表格 wrapper 的 `overflow:auto` 和 `min-width`。不得修改非远程桌面布局。README 说明局域网 URL 和成品 FRP 必须以 HTTPS 转发至安全隧道模式显示的 `127.0.0.1:<port>`，不得使用转发头绕过密码。

- [ ] **Step 4: 运行全量验证与真实 HTTP 冒烟测试。**

Run: `go test ./...`

Run: `npm test`

Run: `npm run build`

Expected: 三个命令均 exit code 0。

启动 `wails dev`，启用随机可用端口后验证：未认证 `POST /api/rpc/FarmAutomationState` 为 401；登录后无 CSRF 为 403；携带会话和 CSRF 为 200；390x844 视口显示底部导航、表格横向滚动和全屏弹窗。

- [ ] **Step 5: 提交。**

Run: `git add frontend/src/style.css frontend/src/components/AppShell.test.tsx README.md`

Run: `git commit -m "feat: optimize remote WebUI for mobile"`

## Plan Self-Review

- 规格覆盖：Task 1-5 覆盖监听、设置、持久化、密码和热切换；Task 3-4 覆盖会话、CSRF、来源、限流和防绕过；Task 6 覆盖业务白名单；Task 7-9 覆盖现有界面复用、密码门、移动端和文档。
- 安全一致性：隧道模式只监听回环，全部转发头不参与授权；桌面文件、进程、补丁、退出和托盘方法同时从 UI 和 RPC map 移除。
- 类型一致性：`LANAccessSettingsInput` 只在桌面表单和 App 边界传密码；`LANAccessStatus` 与 `LANAccessPublicSettings` 不含 hash；RPC 参数始终为 `[]json.RawMessage`。
- 完整性：每个生产改动之前都有失败测试和精确验证命令；计划中没有任何待填占位标识或后续未定义阶段。

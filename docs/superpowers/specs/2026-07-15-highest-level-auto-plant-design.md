# 自动种植最高等级作物设计

## 目标

在自动种植的主、次策略下拉菜单中提供“最高等级作物”，使用户能够选择现有的 `highest_level` 后端策略。

## 行为

- 选项值为 `highest_level`，显示文本为“最高等级作物”。
- 选项位于“背包优先”之后，与其余分析型策略保持同组和相同排序。
- 已有后端会在背包和商店可用种子中选择等级最高的作物，并继续使用既有主、次策略回退规则。

## 范围

仅修改前端策略选项和前端渲染回归测试。后端已经支持该值，因此不修改其策略解析或选种逻辑。
